name: jal_dharan
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  fl_chart: ^0.68.0
  image_picker: ^1.1.0

  # Firebase — all from BoM 4.9.0 (fully compatible)
  firebase_core: 4.4.0
  firebase_auth: 6.1.4
  cloud_firestore: 6.1.2        # ← exact BoM 4.9.0 version
  firebase_storage: 13.0.6      # ← exact BoM 4.9.0 version

  google_sign_in: ^6.2.2
  sign_in_with_apple: ^6.1.2
  crypto: ^3.0.3
  http: ^1.1.0
  flutter_map: ^7.0.0
  latlong2: ^0.9.1
  socket_io_client: ^2.0.0
  provider: ^6.0.0
  google_maps_flutter: ^2.9.0
  maps_toolkit: ^3.1.0
  geolocator: ^13.0.1
  permission_handler: ^11.3.1
  geocoding: ^4.0.0
  ar_flutter_plugin_updated: ^0.0.1
  path_provider: ^2.1.5
  vector_math: ^2.1.4
  flutter_tts: ^4.2.5
  url_launcher: ^6.3.2
  flutter_typeahead: ^5.2.0
  pdf: ^3.11.3
  printing: ^5.14.2
  flutter_3d_controller: ^2.3.0
  phosphor_flutter: ^2.1.0
  intl: ^0.20.2
  shared_preferences: ^2.2.0
  flutter_secure_storage: ^9.0.0
  introduction_screen: ^3.1.14
  wifi_scan: ^0.4.1
  network_info_plus: ^5.0.0
  flutter_localizations:
    sdk: flutter
  speech_to_text: ^7.3.0
  model_viewer_plus: 1.9.3
  
  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/
    - assets/images/
    - assets/icons/
    - assets/models/house_model.glb
    - assets/recharge_shaft_with_pit.glb
    - assets/recharge_Trench.glb